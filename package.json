{
  "name": "course-review",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd api && npm run dev\" \"cd client && npm start\" ",
    "installation": "npm install && cd api && npm install && cd .. && cd client && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.2.0"
  },
  "dependencies": {
    "lodash": "^4.17.19",
    "react-papaparse": "^3.4.0"
  }
}
